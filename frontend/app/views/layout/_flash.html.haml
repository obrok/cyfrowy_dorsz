#flash{ :style => "display:none" }
  - if message[:notice]
    .notice
      %img{ :src => '/images/tick.png' }
      %span.message=h message[:notice]
  - elsif message[:error]
    .error
      %img{ :src => '/images/error.png' }
      %span.message=h message[:error]