= form(:action => action, :method => :get) do
  - if @teachers.empty?
    %p Możesz dodać pytanie o prowadzącego aby filtrować wyniki.
  - else
    %fieldset
      .ctrl
        = select(:name => "teacher_id", :label => "Pokaż odpowiedzi dla tego prowadzącego", :collection => @teachers, :prompt => "Wszyscy")
      .ctrl.buttons
        = submit("Pokaż")

    
